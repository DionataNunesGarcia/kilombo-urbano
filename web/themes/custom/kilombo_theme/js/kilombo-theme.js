/**
 * @file
 * Kilombo behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.kilomboTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
